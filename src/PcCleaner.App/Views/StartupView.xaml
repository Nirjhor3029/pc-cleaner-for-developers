<UserControl x:Class="PcCleaner.App.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20" Background="#0f0f1e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="🚀 Startup Manager" Foreground="White" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Disable unnecessary apps that slow boot — never deletes, only disables. Requires admin for HKLM/Tasks." Foreground="#94a3b8" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,8">
            <Button Content="⟳ Refresh" Width="90" Height="30" Style="{StaticResource MainButtonStyle}" Background="#1f2b4a" FontSize="12" Command="{Binding LoadCommand}"/>
            <TextBlock Text="{Binding StatusText}" Foreground="#b8c0d0" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Entries}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" Background="#16213e" Foreground="White"
                      RowBackground="#16213e" AlternatingRowBackground="#1a2340" GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Application" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Impact" Binding="{Binding Impact}" Width="90"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="130"/>
                    <DataGridTemplateColumn Header="Action" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Disable" FontSize="11" Height="22" Padding="8,0" Background="#533483" Foreground="White" BorderThickness="0"
                                            Command="{Binding DataContext.DisableCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Enable" FontSize="11" Height="22" Padding="8,0" Background="#0f3460" Foreground="White" BorderThickness="0" Margin="4,0,0,0"
                                            Command="{Binding DataContext.EnableCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
